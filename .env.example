# Database
DATABASE_URL="postgresql://nextmarket:nextmarket_dev_password@localhost:5432/nextmarket"

# Auth.js v5
AUTH_SECRET="generate-with-openssl-rand-base64-32"
AUTH_URL="http://localhost:3000"

# App
NEXT_PUBLIC_APP_URL="http://localhost:3000"

# Exchange rate API (only used in hardened variant)
EXCHANGE_RATE_API_URL="https://api.exchangerate.host/latest"

# Revalidation token (only used in hardened variant)
REVALIDATION_SECRET="generate-with-openssl-rand-hex-32"
